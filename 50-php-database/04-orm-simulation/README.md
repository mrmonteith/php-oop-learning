# ORM Simulation

Demonstrates a simple ORM-like pattern.

- `BaseModel.php` — manages PDO connection  
- `User.php` — model with save and all methods  
- `example.php` — demo of saving and fetching users
