# Factory Pattern

## 📘 Overview
The Factory pattern provides a clean way to create objects without exposing the creation logic.  
It centralizes object creation and makes your code easier to extend.

## 🧩 Files
- `Factory.php` — contains `VehicleFactory`, `Car`, and `Truck`  
- `example.php` — runnable demonstration  

## ▶️ Running
Visit:

```
http://localhost:8000/../10-design-patterns/Factory/example.php
```
