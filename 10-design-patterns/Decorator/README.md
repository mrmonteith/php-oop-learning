# Decorator Pattern

## 📘 Overview
The Decorator pattern adds new behavior to an object dynamically without modifying the original class.

## 🧩 Files
- `Notifier.php` — base class  
- `EmailDecorator.php` — decorator adding email functionality  
- `example.php` — runnable demonstration  

## ▶️ Running
```
http://localhost:8000/../10-design-patterns/Decorator/example.php
```
