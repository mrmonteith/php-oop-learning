# Strategy Pattern

## 📘 Overview
The Strategy pattern allows you to change an algorithm or behavior at runtime by swapping strategy objects.

## 🧩 Files
- `Strategy.php` — contains strategies and the `Checkout` context  
- `example.php` — runnable demonstration  

## ▶️ Running
Visit:

```
http://localhost:8000/../10-design-patterns/Strategy/example.php
```
