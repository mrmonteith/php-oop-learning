# Observer Pattern

## 📘 Overview
The Observer pattern allows multiple objects to be notified automatically when another object changes state.

## 🧩 Files
- `Observer.php` — contains `Observer`, `User`, and `NotificationCenter`  
- `example.php` — runnable demonstration  

## ▶️ Running
Visit:

```
http://localhost:8000/../10-design-patterns/Observer/example.php
```
