# SPL Interfaces

## 📘 Overview
SPL interfaces like `Iterator` and `Countable` allow objects to behave like arrays and collections.

## 🧩 Files
- `ItemCollection.php` — implements `Iterator` and `Countable`  
- `example.php` — runnable demonstration  

## ▶️ Running
```
http://localhost:8000/../14-spl-interfaces/example.php
```
