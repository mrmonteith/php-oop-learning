# Interfaces

## 📘 Overview
This example shows how interfaces define contracts that classes must implement, allowing consistent behavior across different implementations.

## 🧩 Files
- `LoggerInterface.php` — interface defining the `log()` method  
- `FileLogger.php` — class implementing `LoggerInterface`  
- `example.php` — runnable demonstration  

## ▶️ Running
Visit:

http://localhost:8000/../05-interfaces/example.php
