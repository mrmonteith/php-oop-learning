# External Service Integration

Demonstrates wrapping an external API in a service class.

- `WeatherService.php` — fetches weather data from Open-Meteo  
- `example.php` — uses the service to display temperature
