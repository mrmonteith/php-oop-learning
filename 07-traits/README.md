# Traits

## 📘 Overview
This example demonstrates traits, which allow you to reuse sets of methods across multiple classes without using inheritance.

## 🧩 Files
- `LoggerTrait.php` — trait providing a `log()` method  
- `Application.php` — class using `LoggerTrait`  
- `example.php` — runnable demonstration  

## ▶️ Running
Visit:

http://localhost:8000/../07-traits/example.php
