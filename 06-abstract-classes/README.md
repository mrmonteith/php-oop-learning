# Abstract Classes

## 📘 Overview
This example demonstrates abstract classes, which provide a base structure with methods that must be implemented by child classes.

## 🧩 Files
- `Shape.php` — abstract class with `getArea()`  
- `Circle.php` — concrete class extending `Shape` and implementing `getArea()`  
- `example.php` — runnable demonstration  

## ▶️ Running
Visit:

http://localhost:8000/../06-abstract-classes/example.php
