# Inheritance

## 📘 Overview
This example demonstrates class inheritance in PHP, where a child class extends a parent class and can override its behavior.

## 🧩 Files
- `Animal.php` — base class with a `speak()` method  
- `Dog.php` — child class extending `Animal` and overriding `speak()`  
- `example.php` — runnable demonstration  

## ▶️ Running
Visit:

